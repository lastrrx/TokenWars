{
  "name": "token-betting-frontend",
  "version": "1.0.0",
  "description": "Frontend for Solana Token Betting Platform",
  "scripts": {
    "dev": "live-server --port=8080",
    "build": "mkdir -p ../dist && cp -r * ../dist/",
    "test": "jest"
  },
  "dependencies": {
    "@solana/web3.js": "^1.87.0",
    "@solana/wallet-adapter-base": "^0.9.23",
    "@solana/wallet-adapter-phantom": "^0.9.24",
    "@solana/wallet-adapter-solflare": "^0.6.28",
    "axios": "^1.6.0",
    "chart.js": "^4.4.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "jest": "^29.7.0"
  }
}
